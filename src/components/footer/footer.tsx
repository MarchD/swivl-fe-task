import React, { memo } from 'react';
import { FooterStyled } from './footer-styled';

function Footer() {
  return (
    <FooterStyled>
      FE TASK
    </FooterStyled>
  );
}

export default memo(Footer);
